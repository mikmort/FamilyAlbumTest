import FaceReview from '@/components/FaceReview';

export const metadata = {
  title: 'Face Review - Family Album',
  description: 'Review and confirm detected faces in photos'
};

export default function FaceReviewPage() {
  return <FaceReview />;
}
