# Azure SQL Database Configuration
# Replace these values with your actual Azure SQL credentials
AZURE_SQL_SERVER=your-server.database.windows.net
AZURE_SQL_DATABASE=your-database-name
AZURE_SQL_USER=your-username
AZURE_SQL_PASSWORD=your-password

# Azure Blob Storage Configuration
AZURE_STORAGE_ACCOUNT=yourstorageaccount
AZURE_STORAGE_KEY=your-storage-access-key
AZURE_STORAGE_CONTAINER=family-album-media

# Email Notification Configuration (OPTIONAL)
# Configure ONE of the following email services to enable email notifications:
# 
# Option 1: Azure Communication Services (recommended for Azure deployments)
# AZURE_COMMUNICATION_CONNECTION_STRING=endpoint=https://...;accesskey=...
# EMAIL_FROM_ADDRESS=DoNotReply@your-domain.azurecomm.net
#
# Option 2: SendGrid (free tier available)
# SENDGRID_API_KEY=SG.xxxxxxxxxxxxx
# EMAIL_FROM_ADDRESS=noreply@your-verified-domain.com
#
# Site URL (required for approval links, usually auto-detected)
# SITE_URL=https://your-app.azurestaticapps.net

# Development Mode (for testing and GitHub Copilot)
# Set DEV_MODE=true to bypass authentication for local testing
# This is useful for automated tests and GitHub Copilot development
# IMPORTANT: Never set this to true in production!
DEV_MODE=false
DEV_USER_EMAIL=dev@example.com
DEV_USER_ROLE=Admin
